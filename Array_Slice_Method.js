const fruits = ['Banana', 'orange','Lemon','Kiwi','Apple','Mango']

fruits.slice(2 , 5) // it will slice the index from 2 to 4 and return the new array with sliced elements left the last mentioned element

console.log(fruits)
